name: zxplore_app
description: A Cross Platform Mobile app for Account Creation By Zenith Staff

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.1.8+10

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.4
  intl: ^0.19.0
#  intl: ^0.18.1
  http: ^1.2.0
#  http: ^0.13.4
#  crypto: ^3.0.1
  crypto: ^3.0.3
  rxdart: ^0.27.7
#  rxdart: ^0.27.3
#  sqflite: ^2.0.1
  sqflite: ^2.3.1
#  encrypt: ^5.0.1
  encrypt: ^5.0.3
#  location: ^4.4.0
  location: ^5.0.3
#  equatable: ^2.0.3
#  package_info: ^2.0.2
#  path_provider: ^2.0.8
  equatable: ^2.0.5
  package_info_plus: ^5.0.1
  path_provider: ^2.1.2
#  url_launcher: ^6.0.17
  url_launcher: ^6.2.4
#  image_picker: ^0.8.4+4
  cupertino_icons: ^1.0.3
  image_picker: ^1.0.7
#  another_flushbar: ^1.10.28
#  shared_preferences: ^2.0.11
  another_flushbar: ^1.12.30
  shared_preferences: ^2.2.2
#  flutter_secure_storage: ^5.0.2
  flutter_secure_storage: ^9.0.0
  # meta: ^1.7.0
  # catcher: ^0.6.9
  # collection: ^1.15.0-nullsafety.4
  
dev_dependencies:

  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/zenithLogo.svg
    - assets/accountType.json
    - assets/images/logo.png
    - assets/images/zenithheader.jpg
    - assets/images/view_offline.png
    - assets/images/accept_signature.png
    - assets/images/create_account.png
    - assets/server_certificate.crt
